from setuptools import setup, find_packages

setup(
    name='demo_app',
    version='0.0.1',
    description='Custom Frappe app for whitelabelled methods and event emission.',
    author='Your Name',
    author_email='your@email.com',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
